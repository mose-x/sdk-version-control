# Wails-generated bindings: managed by `wails generate module`, reformatted
# only creates churn against the generator's tab-based output.
frontend/wailsjs/

# Not prettier's job: Go is formatted by gofmt (go:fmt stage), shell scripts
# are checked with bash -n. Prettier has no parser for these and exits with
# an error if they are passed explicitly.
*.go
*.sh
.prettierignore
